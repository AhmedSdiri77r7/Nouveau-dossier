name: Build & Deploy Spring Boot to Azure

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      PROJECT_DIR: budget-backend-ahmed

    steps:
      # 1) Checkout du code
      - name: Checkout
        uses: actions/checkout@v4

      # 2) Login à Azure avec le secret AZURE_CREDENTIALS
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # 3) Setup Java 17
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # 4) Cache Maven
      - name: Cache Maven
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # 5) Build du backend (le frontend doit déjà être copié dans static)
      - name: Build with Maven
        run: |
          cd $PROJECT_DIR
          mvn -B clean package -DskipTests

      # 6) Préparer un ZIP pour Azure
      - name: Package ZIP
        run: |
          cd $PROJECT_DIR/target
          rm -f app.zip
          zip app.zip *.jar
          ls -l app.zip
          cd -

      # 7) Déployer sur Azure Web App
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: budget-backend-ahmed
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ${{ env.PROJECT_DIR }}/target/app.zip
